(ns tidy-tuesdays.tidy-tuesdays
  "FIXME: my new org.corfield.new/scratch project.")

(defn exec
  "Invoke me with clojure -X tidy-tuesdays.tidy-tuesdays/exec"
  [opts]
  (println "exec with" opts))

(defn -main
  "Invoke me with clojure -M -m tidy-tuesdays.tidy-tuesdays"
  [& args]
  (println "-main with" args))
